apiVersion: v1
kind: Service
metadata:
  name: {{ include "feedfarmer.fullname" . }}
  labels:
    {{- include "feedfarmer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "feedfarmer.selectorLabels" . | nindent 4 }}
